# Workspace

This file is the agent's durable scope. Keep it concise and update it only from
owner-confirmed context. Put received files in `inbox/` and working artifacts in
`work/`. Never store credentials here.
